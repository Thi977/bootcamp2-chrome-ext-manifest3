{
  "name": "focus-tracker-mvp",
  "version": "1.0.0",
  "description": "Rastreador de Foco MV3 com testes E2E.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-extension.mjs",
    "test:e2e": "playwright test --config=tests/playwright.config.js --reporter=list,html",
    "test": "npm run build && npm run test:e2e",
    "ci": "npm ci && npm run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "@types/chrome": "^0.1.24",
    "archiver": "^7.0.0"
  }
}
